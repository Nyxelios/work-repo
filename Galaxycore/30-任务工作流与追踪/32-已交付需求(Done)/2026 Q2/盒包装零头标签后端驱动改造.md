---
title: 盒包装零头标签后端驱动改造
date: 2026-05-20
status: completed
tags: [MES, 工作流, 盒包装, 零头标签]
---

# 盒包装零头标签后端驱动改造

## 背景

原零头标签打印逻辑在前端计算，使用包装前传入的 `lotInfoList` 数据扣减已包装数量。该方式存在以下问题：

1. **数据不准确**：前端使用的是包装前的旧数据，如果包装过程中数量发生变化，计算结果会偏差
2. **OTHERS 场景遗漏**：当盒内批次超过显示上限（COM 5个/FT 4个）合并为 "OTHERS" 时，前端无法准确知道具体哪些批次在 OTHERS 中
3. **前后端数据不一致**：前端计算逻辑与后端实际包装结果可能不一致

## 改造目标

- 零头标签的打印完全由后端驱动
- 前端不再做任何零头数量计算
- 所有包装操作统一返回 `remainingLots`

## 改造内容

### 1. 后端改造

#### 1.1 整包/尾数包 (`doBoxPackageLot`)

包装完成后，遍历所有参与包装的批次，通过 `LotDieBin` 查询数据库最新的未包装数量，组装 `remainingLots` 返回：

```java
JSONArray remainingLotJson = new JSONArray();
for (Lot lot : lotsInfo) {
    LotDieBin lotDieBin = lotService.getLotDieBinByRrn(lot.getLotDieBinRrn());
    if (lotDieBin.getUnpackedQty() != null && lotDieBin.getUnpackedQty() > 0) {
        JSONObject remaining = new JSONObject();
        remaining.put("lotId", lot.getLotId());
        remaining.put("unpackedQty", lotDieBin.getUnpackedQty().intValue());
        remaining.put("grade", lot.getWaferLevel());
        remaining.put("productId", productId);
        remaining.put("workOrderId", workOrderId);
        remainingLotJson.add(remaining);
    }
}
map.put("remainingLots", remainingLotJson);
```

#### 1.2 散包装 (`boxLoosePackage`)

同样补充 `remainingLots` 返回，确保所有包装操作统一：

```java
// 与整包相同的 remainingLots 组装逻辑
JSONArray remainingLotJson = new JSONArray();
for (Lot lot : lotsInfo) {
    LotDieBin lotDieBin = lotService.getLotDieBinByRrn(lot.getLotDieBinRrn());
    if (lotDieBin.getUnpackedQty() != null && lotDieBin.getUnpackedQty() > 0) {
        // ... 组装 remainingLots
    }
}
map.put("remainingLots", remainingLotJson);
```

### 2. 前端改造

#### 2.1 调用方式统一

两个包装入口都改为直接传递 `responseJson.msg.remainingLots`：

```javascript
// 整包/尾数包
printRemainingLotLabel(responseJson.msg.remainingLots);

// 散包
printRemainingLotLabel(responseJson.msg.remainingLots);
```

#### 2.2 `printRemainingLotLabel` 函数简化

去掉所有前端计算逻辑，只做最简单的判断：

```javascript
function printRemainingLotLabel(remainingLots) {
    if (!remainingLots || remainingLots.length == 0) {
        return;  // 没有零头，直接返回不打印
    }

    var remainRecord = remainingLots[0];
    var printInfo = "GRADE=" + remainRecord.grade
        + ";LOTID=" + remainRecord.lotId
        + ";NUM=" + remainRecord.unpackedQty
        + ";PRODUCTID=" + remainRecord.productId
        + ";WORKORDERID=" + remainRecord.workOrderId;
    bartenderPrint(printInfo, "packageLotInfo.btw", 1);
}
```

## 核心规则

| 规则 | 说明 |
|------|------|
| 前端不计算 | 前端不做任何零头数量计算，完全依赖后端返回的数据 |
| 有数据就打印 | 后端返回 `remainingLots` 数组有元素，就打印零头标签 |
| 没数据就不打印 | 后端返回空数组，表示所有批次已包完，没有零头 |
| 所有包装操作统一 | 整包、尾数包、散包三个操作的后端接口都返回 `remainingLots` |

## 修改文件

### 后端
- [PackageLotAction.java](file:///d:/data/document/Github/work-archive/Code-Projects/gc/core/src/model/src/com/mycim/webapp/actions/wip/PackageLotAction.java)
  - `doBoxPackageLot` 方法：补充 `remainingLots` 返回
  - `boxLoosePackage` 方法：补充 `remainingLots` 返回

### 前端
- [packageLotGrid.js](file:///d:/data/document/Github/work-archive/Code-Projects/gc/core/web/wip/packageLot/packageLotGrid.js)
  - `doPackage` success 回调：改为传递 `remainingLots`
  - `doLoosePackage` success 回调：改为传递 `remainingLots`
  - `printRemainingLotLabel` 函数：去掉前端计算，纯后端驱动

## 关联任务

- 源自：[盒包装优化查询以及出零头标签](../TODO/TBC/盒包装优化查询以及出零头标签.md)（TBC）
- 本工作流的后台/功能改动部分，对应 TBC 中的"包装时使用的批次，如果有零头就打印一张零头的批次标签"

## 相关文档

- [盒包装功能文档](盒包装.md)

## 验证要点

1. 整包后批次有剩余 → 应打印零头标签，数量准确
2. 整包后批次无剩余 → 不应打印零头标签
3. 散包后批次有剩余 → 应打印零头标签，数量准确
4. 散包后批次无剩余 → 不应打印零头标签
5. 尾数包后批次有剩余 → 应打印零头标签，数量准确
